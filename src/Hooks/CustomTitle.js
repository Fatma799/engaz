import React from 'react';

const CustomTitle = ({ title }) => {
    return (
        <h2 className="text-3xl flex justify-center items-center mb-10">{title}</h2>
    );
};

export default CustomTitle;